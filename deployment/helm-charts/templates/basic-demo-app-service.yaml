apiVersion: v1
kind: Service
metadata:
  labels:
    app: basic-demo-app
  name: basic-demo-app
  namespace: {{ .Values.global.namespace }}
spec:
  ports:
    - port: 8083
      targetPort: 8083
      name: http
  selector:
    app: basic-demo-app